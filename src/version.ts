/**
 * Versão anunciada aos provedores (User-Agent do Claude, `clientInfo` do Codex).
 * Precisa acompanhar o `version` do `package.json` — há um teste que falha se divergir.
 */
export const VERSION = '0.1.1';
